spring:
  application:
    name: SpringbootRealtimeProudApplicationEndToEnd
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/springbootrealtimeendtoend
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8082    
logging:
  file:
    name: C:/Users/Welcome/OneDrive/Desktop/logs/SpringbootRealTimeEndToEnd.log
    
management:
  tracing:
    sampling:
      probability: 1.0